name: salt_shared
description: Shared recipe models, YAML codec, and parsers for SaltToTaste (server + app)
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  dart_mappable: ^4.5.0
  yaml: ^3.1.2

dev_dependencies:
  build_runner: ^2.4.13
  dart_mappable_builder: ^4.5.0
  lints: ^5.1.1
  test: ^1.25.8
